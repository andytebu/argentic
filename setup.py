from setuptools import setup, find_packages

setup(
    name="argentic",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Añade aquí las dependencias del proyecto
    ],
)
